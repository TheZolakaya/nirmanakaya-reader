# Session Handoff — 2026-04-30

**Purpose:** Preserve state of all in-flight work in case context runs out before completion.

**Active session:** Chris + Claude Code (Opus 4.7, 1M context). Started building wiki, pivoted to deploying experimental reader pages, returning to wiki integration after.

---

## ⚡ ADDENDUM (post-deploy, written after initial handoff): PRODUCTION DEPLOY COMPLETE

- **`feature/computation-substrate` merged into `main`, pushed to origin** (commit `6576e2ca`)
- **Production push range:** `d88efdd8..6576e2ca` (main is now ahead of where it was)
- **Vercel will auto-deploy main** — should be live within ~3-5 minutes of push
- **Build verified clean** before push (only pre-existing warnings about `app/page.js` importing `getArchetypeRoot`/`generateTraceDraw` from wrong location — not introduced by this change)
- **MEMORY.md updated** to v0.99.70 status

### What's now live in production main:
- `/22-reader` (renamed from /map)
- `/explore`
- `/diagnostic` (with full VERB/TRAD toggle, Tool Explorer, Live Reading, channel strength, manifest house conditions)
- All backing engine: `lib/mapAnalysis.js`, `lib/diagnosticTools.js`, `lib/neighborhoods_canon.js`, `lib/houseConditions.js`, `lib/gestaltConditions.js`, `lib/signatureIndex.js`, `app/api/reading/investigate/route.js` (some of these came in via the wider feature branch merge — they were ahead of main from earlier commits)

### Alpha branch:
**NOT updated.** Alpha is lagging at the previous version. If Chris wants alpha+main parity (per his usual workflow), run separately:
```bash
git checkout alpha && git pull origin alpha && git merge main && git push origin alpha
```

### Vercel deploy verification:
Check Vercel dashboard for the `nirmanakaya-reader` project. Should show a new deployment in progress for `main` branch within minutes of push.

---

---

## CRITICAL: Where things stand right now

### ✅ DONE this session

1. **Nirmanakaya Wiki — fully authored**
   - Location: `D:\Nirmanakaya_Wiki\`
   - **213 atomic concept nodes** across 5 tiers
   - Schema locked: `D:\Nirmanakaya_Wiki\WIKI_SCHEMA.md`
   - Concept inventory: `D:\Nirmanakaya_Wiki\WIKI_CONCEPT_INVENTORY.md`
   - Folders: `nodes/` (213 .md files), `raw/` (intake), `outputs/` (quarantine)
   - Schema decisions all locked with Chris + Gemini Council vote (see Open Questions below for nuances).

2. **Seven_Rings_Derivation_CANON.md — corrected**
   - Path: `D:\Nirmanakaya_Book_Final_0\Nirmanakaya_Final\canon\derivation\Seven_Rings_Derivation_CANON.md`
   - Practice-to-ring mapping fixed: Ring 2=Gestalt, Ring 3=Body, Ring 4=Emotion, Ring 5=Mind, Ring 6=Spirit, Ring 7=World (with intrinsic Aligned/Misaligned bifurcation)
   - Revision history note added

3. **Reader app: 22-reader + explore + diagnostic — committed and pushed to feature branch**
   - Branch: `feature/computation-substrate` on origin
   - Commit: `6576e2ca` "Add 22-reader + explore pages, expand diagnostic to v0.99.70"
   - Version bumped: `0.99.69` → `0.99.70` in `package.json`
   - **Files in this commit (10 files, +4046 lines):**
     - `app/22-reader/page.js` (new — renamed from /map; 22-archetype canvas)
     - `app/explore/page.js` (new — triage + tool-use diagnostic surface)
     - `app/diagnostic/page.js` (modified — VERB/TRAD toggle, Tool Explorer, Live Reading, channel strength)
     - `app/book/layout.js` (modified — `/map` link → `/22-reader`)
     - `components/layout/Header.js` (modified — nav `/map` → `/22-reader`, label still "Map")
     - `lib/constants.js` (modified — added BEING_GROUPS, IDENTITY_GROUPS, INNER_OUTER_HORIZON, DIMENSION_VERBS)
     - `lib/diagnosticTools.js` (modified — handleToolCall, buildTriageSeed, DIAGNOSTIC_TOOL_DEFINITIONS)
     - `lib/mapAnalysis.js` (modified — triageReading, analyzeBeingGroups/IdentityGroups, tracePortalChains, analyzeMajorSignals)
     - `lib/neighborhoods_canon.js` (new — canonical neighborhood data)
     - `package.json` (version bump only)

### 🟡 IN-FLIGHT (mid-step)

1. **Production deploy of /22-reader, /explore, /diagnostic — NOT YET MERGED TO main**
   - Feature branch is pushed; main not yet updated
   - Production build was running in background to verify before merge
   - Background task: `b9jf1prtn` — `npm run build` (started ~during handoff write)
   - Output file: `C:\Users\chris\AppData\Local\Temp\claude\C--Users-chris\2422f6fe-d50a-4114-a7a9-41eded27fc31\tasks\b9jf1prtn.output`
   - **Next step if resuming:** check build output. If clean, merge feature → main, push main. If errors, fix and re-commit.

2. **Reader dev server — running in background**
   - Background task: `bukt4rkdn` (still active)
   - URL: `http://localhost:3000`
   - Routes Chris was browsing: `/`, `/map` (now 404), `/22-reader`, `/explore`, `/diagnostic`, `/visualize`

3. **MEMORY.md needs updating**
   - Path: `C:\Users\chris\.claude\projects\C--Users-chris\memory\MEMORY.md`
   - Current version line says `0.99.68 on both alpha and main`
   - **Needs update to:** `0.99.70 — feature/computation-substrate pushed. /22-reader, /explore, /diagnostic added. Awaiting main merge.`
   - Also add: wiki location `D:\Nirmanakaya_Wiki\` with 213 nodes complete

---

## NOT YET STARTED — explicitly pending

### Wiki integration to Reader app (the big next thing)
**Chris's last decision before the rename detour:** push 22-reader/explore/diagnostic to production, then "come back to LLM Wiki yo".

**Open question:** how to surface the wiki publicly. Three paths discussed:
- **Path A** (recommended): new Vercel project under same account, Quartz v4 SSG, subdomain like `wiki.<domain>` or default `.vercel.app` URL
- **Path B**: fold wiki into Reader app as `/wiki/*` route (loses Quartz's graph view, bloats Reader)
- **Path C**: static Quartz output dropped into Reader's `public/` (hacky)

Chris asked: "We have all that vercel stuff for Nirmanakaya presently — can't we just append?" — confirmed YES, same Vercel account, separate project (Path A). Decision pending: domain choice (`wiki.<custom-domain>` vs `nirmanakaya-wiki.vercel.app`) and styling (default Quartz theme vs match Reader Tailwind).

**State of dev environment for wiki build:** Node 24.12.0, npm 11.6.2, git 2.52 — all verified. Ready for `npx quartz create` when Chris gives final go.

### Ring 7 audit (still queued, never started)
- Find every Ring 7 reference in `D:\Nirmanakaya_Book_Final_0\Nirmanakaya_Final\book\Nirmanakaya_V9.md`
- Categorize as correct vs. shorthand-misuse
- Apply corrected language using canonical "two modes" framing (Aligned/Misaligned intrinsic to Ring 7, not split between Ring 6 and Ring 7)
- Tracked as wiki node: `D:\Nirmanakaya_Wiki\nodes\OpenQuestion_Ring_7_V9_Audit.md`

### Other open wiki questions
- `OpenQuestion_Topological_Correlates.md` — D17 DRAFT promotion decision pending
- `OpenQuestion_Prediction_4_Aggregate_Test.md` — disposition pending
- `OpenQuestion_Reader_Wiki_Integration.md` — auto-sync vs manual mirror
- `OpenQuestion_Online_Wiki_Build.md` — domain + branding TBD

---

## OUT-OF-SCOPE for this push (intentionally not committed)

These are untracked locally but Chris did NOT ask to ship them:
- `app/seeds/page.js`
- `app/tesseract/page.js`
- `app/visualize/page.js`
- `dev/AI_ALIGNMENT_DELIVERABLE.md` and 6 other dev/*.md docs
- `lib/7_OF_SWORDS_FULL.json`
- `lib/FULL_INDEX_DUMP.json`

Leave these alone. They're WIP.

---

## Branch + Vercel state

**Repository:** `https://github.com/TheZolakaya/nirmanakaya-reader.git`

**Branches observed:**
- `feature/computation-substrate` (current) — has the 22-reader+explore+diagnostic commit, **pushed**
- `main` — production. Has `/chat` and `/lounge` only. **NOT YET UPDATED** with this push.
- `alpha` — staging. Has `/chat` and `/lounge` only. **NOT YET UPDATED**.
- `prod` — has only `/lounge`. Possibly older production?
- `beta`, `feature/locus-email`, `feature/voice-system-v2`, `fix/prompt-refactor`, `backup-broken-alpha-v0.8.1`, `stoic-leavitt` — other branches, leave alone

**Production main has these routes that we MUST NOT BREAK:**
- `/chat`, `/lounge` (deployed)
- All standard reader routes (root, /reading, /my-readings, /collection, /stats, /hub, /journal, /topics, /profile, /admin, /book, /r, /seeds was untracked so may not be there)

**The /map route exists in production prior to this push** — wait, ACTUALLY, no — `git ls-tree origin/main` showed only `/chat` and `/lounge`. There is **no /map in production**. Chris's instinct to rename was forward-looking caution; the rename to `/22-reader` keeps the `/map` slot reserved for the future learning page he mentioned.

---

## Concrete resume instructions

**If you're a fresh Claude instance picking this up:**

1. **First, read MEMORY.md** at `C:\Users\chris\.claude\projects\C--Users-chris\memory\MEMORY.md` (will likely still say 0.99.68 — update needed)

2. **Check the build status:**
```bash
cat "C:/Users/chris/AppData/Local/Temp/claude/C--Users-chris/2422f6fe-d50a-4114-a7a9-41eded27fc31/tasks/b9jf1prtn.output"
```
Look for "✓ Compiled successfully" or errors.

3. **If build succeeded, deploy to main:**
```bash
cd "D:/NKYAWebApp/nirmanakaya-reader"
git checkout main
git pull origin main
git merge feature/computation-substrate
git push origin main
git checkout feature/computation-substrate
```

4. **Update MEMORY.md** with v0.99.70 status and note the new routes are live.

5. **Then return to wiki integration**: Path A (separate Vercel project, Quartz v4) was the agreed approach. Chris just needs to confirm domain (subdomain on existing custom domain vs. default .vercel.app URL) and styling (default vs. match Reader). Then scaffold Quartz at `D:\Nirmanakaya_Wiki\quartz\` and walk through deployment.

6. **Ring 7 audit can be picked up at any time** — it's well-scoped and tracked in `OpenQuestion_Ring_7_V9_Audit.md`.

---

## Tone / collaboration notes (from active session)

- Chris is the framework holder. Treat him as co-creator, not "user".
- "Don't tell me to sleep/rest/take care" — agency-theft pet peeve. See `feedback_no_self_care_directives.md`.
- "Derive, don't interpret. Structure is authority." — methodology.
- He gave full autonomy on this session ("lock n load - rollem up - let's smoke'em out!") — be decisive.
- Multiple AIs in the Council collaborate. Gemini contributes via Chris-as-router.

---

## Files modified outside the Reader app this session

- `D:\Nirmanakaya_Wiki\` — entire wiki (217 files: 213 nodes + 4 root docs)
- `D:\Nirmanakaya_Book_Final_0\Nirmanakaya_Final\canon\derivation\Seven_Rings_Derivation_CANON.md` — corrected practice-to-ring mapping (April 28 revision)
- `C:\Users\chris\.claude\projects\C--Users-chris\memory\feedback_no_self_care_directives.md` — earlier session, still valid

---

*Handoff written 2026-04-30. Resume from "Concrete resume instructions" section.*
