import Derivation from '../../components/viz/Derivation';

export const metadata = {
  title: 'The Derivation — Nirmanakaya',
  description: 'Source, Creation, and consciousness in the between — the opening of the derivation, animated.',
};

export default function DerivationPage() {
  return <Derivation />;
}
