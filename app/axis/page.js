import AxisOfBecoming from '../../components/viz/AxisOfBecoming';

export const metadata = {
  title: 'The Axis of Becoming — Nirmanakaya',
  description: 'The two portals, the ten fundamental nodes, and the twenty archetypes — a technical drawing of the architecture.',
};

export default function AxisPage() {
  return <AxisOfBecoming />;
}
